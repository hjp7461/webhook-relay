---
measurement:
  id: 2026-05-27T06-46-26Z_LP-1_N1
  started_at: 2026-05-27T06:46:54Z
  ended_at: 2026-05-27T06:53:28Z
  git_commit: 11bdd0d003b2570e0da2301256e3bf3dab2ff58e
  hostname: hongjeongpyoui-MacBookPro-2.local
  host:
    cpu_model: "Intel(R) Core(TM) i7-8750H CPU @ 2.20GHz"
    cpu_ghz: 2.20
    ram_gb: 32
    os: "macOS 15.7.7 (Darwin 24.6.0)"
  docker_version: "Server 29.5.2 (Client 29.5.2)"
  k6_version: "k6 v2.0.0+dirty (commit/8c3be52cc1-dirty, go1.26.3, linux/amd64)"
  cgroup:
    api:        { cpus: ~, mem: ~ }
    worker:     { cpus: ~, mem: ~, instances: 1 }
    redis:      { cpus: ~, mem: ~ }
    prometheus: { cpus: ~, mem: ~ }
    grafana:    { cpus: ~, mem: ~ }
    k6:         { cpus: 2.0, mem: "1G" }
  redis_topology: "single-instance"
  load_profile:
    lp_id: "LP-1"
    rps: 10
    payload_distribution: { small: 1.0, medium: 0, large: 0 }
    pattern: "steady"
    window:
      warmup_s: 60
      load_s: 300
      cooldown_s: 30
---

# LP-1 baseline 측정 결과 (2026-05-27)

본 보고서는 4단계 M-LOAD-2 의 첫 측정 결과 + 분산 검증 cross-check.
PLAN `docs/plan-phase4/03-m-load-2-lp1-baseline.md` §4 단계 5 정합.
형식은 `docs/prd-phase4/results/README.md` §3 단일 출처.

## 1. 측정 윈도우 (W_load = SLI 계산 범위)

| | Run 1 (본 측정) | Run 2 (분산 검증) |
|--|------------------|-------------------|
| t_start | `2026-05-27T06:47:56Z` | `2026-05-27T06:55:12Z` |
| t_end | `2026-05-27T06:52:58Z` | `2026-05-27T07:00:15Z` |
| 지속 | 302초 (~5분) | 303초 |

## 2. SLI 결과 (Run 1, 본 측정)

PromQL 결과는 W_load 안의 21 step (15s 간격) 시계열 분포의 통계.
SLO-1 / SLO-4 는 분자(`5xx` / DLQ 적재) 가 0 이라 PromQL vector empty
→ 0 으로 해석.

| SLI | 단위 | p50 | p99 | 평균 | 분산 (max-min) |
|-----|------|-----|-----|------|----------------|
| SLO-1 가용성 (5xx 비율) | % | 0 | 0 | 0 | 0 (5xx 0건) |
| SLO-2 등록 지연 p99 | ms | 4.972 | 4.990 | 4.973 | 0.024 |
| SLO-3 전달 지연 p99 | ms | 9.928 | 9.941 | 9.928 | 0.041 |
| SLO-4 DLQ 적재율 | % | 0 | 0 | 0 | 0 (DLQ 0건) |
| 처리량 (RPS achieved) | RPS | n/a | n/a | 10.003051 | n/a |
| 카디널리티 (시계열 수) | count | n/a | n/a | 165 | 0 |

### 2.1 k6 측 추가 통계 (W_load 5분, 3001 iterations)

| 메트릭 | 값 |
|--------|-----|
| http_reqs | 3001 (= 10.003051 / s) |
| http_req_failed | 0.00% (0 / 3001) |
| http_req_duration avg | 2.263ms |
| http_req_duration p(90) | 2.892ms |
| http_req_duration p(95) | 3.178ms |
| http_req_duration max | 21.837ms |
| iteration_duration avg | 2.500ms |

## 3. 결과 해석 — 3단계 SLO 잠정값 대비

3단계 SLO (PRD `prd-phase3/04` §3.1, Q-OBS-11 (a) 잠정값):

| SLO | 임계 (잠정값) | 실측 (Run 1) | 사용 비율 | 결과 |
|-----|--------------|--------------|-----------|------|
| SLO-1 가용성 ≥ 99.5% | 5xx ≤ 0.5% | 0% | - | ✅ |
| SLO-2 등록 지연 p99 ≤ 500ms | 500ms | 4.99ms | 1.0% | ✅ |
| SLO-3 전달 지연 p99 ≤ 5000ms | 5000ms | 9.94ms | 0.2% | ✅ |
| SLO-4 DLQ 적재율 ≤ 1% | 1% | 0% | - | ✅ |

LP-1 baseline (R = 10 RPS, P = small 1KB 고정) 에서 본 시스템은 SLO
잠정값을 **모두 통과**. 절대값이 임계 대비 매우 낮음 (등록 1.0%, 전달
0.2%) — baseline 영역에서는 SLO 마진을 충분히 보유. M-LOAD-3 nominal
sustained / M-LOAD-4 stress 영역에서의 SLO 사용 비율 변화가 후속 측정의
관심 영역.

## 4. 측정 분산 확인 (Run 1 vs Run 2, PRD `prd-phase4/02` §7.2 ±5% 검증)

| SLI | Run 1 | Run 2 | Δ% | 허용 ±% | 결과 |
|-----|-------|-------|----|---------|------|
| RPS achieved (k6) | 10.003051 | 10.002985 | -0.00066% | ±2% | ✅ |
| SLO-2 등록 p99 (max) | 4.990ms | 4.964ms | -0.52% | ±5% | ✅ |
| SLO-3 전달 p99 (max) | 9.941ms | 9.959ms | +0.18% | ±5% | ✅ |
| SLO-4 DLQ 적재율 | 0 | 0 | 0 | ±10% | ✅ |
| 카디널리티 | 165 | 165 | 0 | 변동 없음 | ✅ |
| 처리량 PromQL avg | 6.581 RPS | 6.681 RPS | +1.5% | ±2% | ✅ |

**모든 핵심 SLI 가 PRD §7.2 분산 임계 안.** 본 측정의 신뢰성 확인.

### 4.1 PromQL 처리량 avg 의 ramp-up 효과

처리량 PromQL avg (6.58 / 6.68 RPS) 가 목표 10 RPS 의 66% 로 표시되는
것은 `sum(rate(...[5m]))` 의 5분 윈도우가 W_load 시작 시점부터 점진적
ramp-up 으로 누적되는 효과. 21 step 의 p99 (9.99 RPS) 가 10.000 근접
→ steady-state 처리량은 정상. 후속 LP-N (W_load 32분 sustained) 에서는
ramp-up 효과 무시 가능.

## 5. Notes — 측정 환경 한계 발견 + 1차 대응

본 보고서의 측정은 **Docker Desktop 재시작 + 호스트 idle 화 직후**의
결과. 본 마일스톤 단계 4 진행 중 다음 환경 노이즈 사례 관찰:

- **첫 시도 (06-20-52Z / 06-31-01Z, 폐기됨):** SLO-3 전달 지연 p99
  분산 -13.5% (Run 1: 33.95ms / Run 2: 29.36ms). PRD §7.2 ±5% 임계
  초과 → 결과 무효 판정 (PLAN §3.3 무효 조건 2).
- **1차 대응 (PRD §7.2):** Docker Desktop 재시작 (호스트의 누적 VM
  상태 + 백그라운드 부하 제거) → 호스트 idle 화 (Chrome / Slack /
  기타 닫기) → 재측정.
- **재측정 (본 보고서):** SLO-3 분산 +0.18%, SLO-2 분산 -0.52% — 모두
  ±5% 안. 측정 신뢰성 회복.

### 5.1 후속 측정 절차 권장 (M-LOAD-3~6)

PRD `prd-phase4/02` §7.3 측정 격리 정책 + 본 노트의 cross-link:

1. 측정 사이클 시작 전 Docker Desktop 재시작 (호스트 부하 누적 시).
2. 호스트 측 백그라운드 idle 화 (Chrome / Slack / IDE 등).
3. baseline ms 영역의 SLO 분산이 노이즈 floor 에 민감 — 측정 환경
   재정리 + 1차 대응 (PRD §7.2) 후 재측정.

## 6. Raw artifact 위치

raw artifact 는 본 마일스톤 단계 6 (PLAN §4 단계 6) 의 사용자 결정
이후 `.gitignore` 처리 예정. 본 Markdown 파일이 측정 결과의 단일 출처
(불변식 I4.10, PRD `02` §6.4).

| 측정 | 위치 |
|------|------|
| Run 1 (본 측정) | `docker/k6/results/LP-1_2026-05-27T06-46-26Z/` |
| Run 2 (분산 검증) | `docker/k6/results/LP-1_2026-05-27T06-53-40Z/` |

각 디렉터리 안: `metadata.yaml`, `k6-warmup.json`, `k6-load.json`,
`prom-queries.json`, `t_start`, `t_end`.

## 7. 회귀 가드 (PLAN §9)

본 측정 사이클 후 회귀 가드 전건 그린 유지:

- `pnpm typecheck` 0 errors (코드 변경 0건 — packages/** 손대지 않음).
- core boundary grep 0 hits (`webhook|delivery|fastify|receiver|_demo|
  Payload` in `packages/core/src/`).
- `docker compose config` 유효 (default + measure profile).
- 카디널리티 165 ≤ 1000 (3단계 IT-OBS-11 가드 안).
- Prometheus targets up=2 유지 (api + worker scrape 정상).

## 8. M-LOAD-3 진입 가능 조건 충족

PLAN §10 "다음 마일스톤 진입 가능 조건":

- [x] §6 Done 정의 전건 통과
- [x] 본 §7 회귀 가드 전건 그린
- [x] LP-1 측정 분산 ±5% 안 확인 (§4)
- [x] AC4.4 측정 프로토콜 통과 (run-lp-1.sh 8 단계 자동화 + 결과 보고서
  Markdown 정합)

→ **M-LOAD-3 진입 준비 완료** (단계 6 `.gitignore` 결정 + 본 마일스톤
closeout 후).
