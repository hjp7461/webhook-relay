---
synthesis:
  id: 2026-05-28T_FINAL_M-LOAD-6
  authored_at: 2026-05-28
  inputs:
    - LP-1_2026-05-27.md  (M-LOAD-2, git_commit 11bdd0d)
    - LP-2_2026-05-27.md  (M-LOAD-3, git_commit 35e018c)
    - LP-3_2026-05-27.md  (M-LOAD-4, git_commit d7f57c8)
    - LP-4_2026-05-28.md  (M-LOAD-4, git_commit 168d63f)
    - horizontal-scaling_2026-05-28.md  (M-LOAD-5, git_commit 7c86b4e)
  metadata_consistency:
    host:           "hongjeongpyoui-MacBookPro-2.local (Intel i7-8750H @ 2.20GHz / 32GB / macOS 15.7.7) — 5 reports identical"
    docker_version: "Server 29.5.2 (Client 29.5.2) — 5 reports identical"
    k6_version:     "k6 v2.0.0+dirty (commit/8c3be52cc1-dirty, go1.26.3, linux/amd64) — 5 reports identical"
    cgroup:         "k6 = 2.0 CPU / 1G mem, others = unlimited — 5 reports identical"
    redis_topology: "single-instance — 5 reports identical"
    git_commit:     "5 reports differ (intentional — each milestone committed its own scenarios/scripts/fixes before measurement)"
  decision_locks_inherited:
    - Q-LOAD-4 (a) Redis 단일 인스턴스 한계 식별 — knee point + HA 트리거
    - Q-LOAD-9 (a) p99 × 1.5 — SLO 재조정 규칙
    - Q-LOAD-10 (b) α=0.8 — SLO-H-1 처리량 선형성
    - Q-LOAD-11 (b) β=1.2 — SLO-H-2 p99 안정성
    - Q-LOAD-12 (a) Markdown 표 — 최종 보고서 형식
  user_decisions_locked_2026-05-28:
    - "git_commit SHA 5 reports difference: 정상 해석 (PLAN §3.4 의 합리적 해석 = 동일 측정 재실행 시 SHA 일관성)"
    - "단계 2/4 빈 commit 회피: final 보고서 본문 흡수 (§A 인라인 commit + §7 체크박스 mark-as-done commit)"
    - "M-LOAD-5 cross-link 4건: 후속 작업으로 분리 (§6 운영 권고에 cross-link 만, 본 PLAN 안에서는 처리하지 않음)"
    - "단계 5 (미세 N 매트릭스 N=3/4/6/7/8): 스킵 (capacity 미달 영역의 추가 측정 정보 가치 낮음)"
    - "SLO-2/SLO-3 baseline (실측 p99): nominal 영역만 (LP-1 / LP-2 normal / M-LOAD-5 N=1) — 가장 보수적인 max 값 채택"
---

# 4단계 PLAN 묶음 최종 종합 보고서 (M-LOAD-6, 2026-05-28)

본 보고서는 4단계 PLAN 묶음 (M-LOAD-1~5) 의 측정 결과를 종합 분석하고,
PLAN `docs/plan-phase4/07-m-load-6-redis-knee-and-final-report.md` §4
단계 1~4 의 산출물 단일 출처다.

> **본 보고서의 책임 (PLAN §4 단계 1):**
> 1. M-LOAD-2~5 결과 보고서의 종합 + 메타데이터 일관성 검증.
> 2. Redis knee point 식별 (PRD `prd-phase4/04` §5.3 절차).
> 3. HA / Cluster 트리거 조건 (T1~T5) 명문화 (PRD §6.2).
> 4. SLO 재조정 임계 산출 (Q-LOAD-9 (a) p99 × 1.5, PRD `03` §4 + §4.5).
> 5. SLO-H-1 / SLO-H-2 검증 결과 종합 (PRD `04` §4).
> 6. 운영 권고 사항 + 후속 작업 cross-link.
> 7. PLAN closeout 체크리스트 (단계 4 commit 에서 mark-as-done).

> **본 보고서가 다루지 않는 것 (Q-LOAD-4 (a) / PLAN §5 정합):**
> Redis HA / Cluster 도입 자체, 카오스 엔지니어링, 부록 트랙, 운영 배포 자동화,
> 외부 수신자 측 성능 측정. 본 보고서는 트리거 조건 명문화 + 후속 PRD/commit
> 시리즈 인계까지만.

---

## 1. 종합 측정 결과 요약

본 §1 은 M-LOAD-2~5 의 5 결과 보고서를 cross-link 하고, 부하 영역별 SLI
분포를 한 표로 종합한다.

### 1.1 측정 결과 보고서 5건 cross-link

| 마일스톤 | 보고서 | 부하 프로필 | 핵심 결과 |
|---------|--------|-------------|-----------|
| M-LOAD-2 | [`LP-1_2026-05-27.md`](./LP-1_2026-05-27.md) | LP-1 (R=10 RPS, P=1KB 고정, W=5분) | baseline. 3단계 SLO 잠정값 4건 전건 통과. Run 1/2 분산 ±0.52% 안 |
| M-LOAD-3 | [`LP-2_2026-05-27.md`](./LP-2_2026-05-27.md) | LP-2 (R=100 RPS, P=80/15/5, W=30분, 4 변형) | nominal sustained. 4 변형 전건 SLO 잠정값 통과 + 결과 무효 조건 보강 (W3 attempts≈3 / SLO-4 DLQ 1.0 / C5 non_retriable rate / D3 80/15/5) 전건 통과 |
| M-LOAD-4 | [`LP-3_2026-05-27.md`](./LP-3_2026-05-27.md) | LP-3 (R=500 RPS, P=64KB 고정, W=30분) | stress. **knee point 명백 진입** — 선형성 0.040 + p99 163× + 큐 waiting 17만. Bound = 단일 Redis fork-time 메모리 (Docker VM 7.65GB 한계). 측정 변수로 기록 |
| M-LOAD-4 | [`LP-4_2026-05-28.md`](./LP-4_2026-05-28.md) | LP-4 (base 100 → spike 1000 × 30s → base 100, total ~13분) | spike. **회복 시간 33.1초** 정상 측정. T3 트리거 미발화 (knee 가 base RPS 안에 들어오지 않음) |
| M-LOAD-5 | [`horizontal-scaling_2026-05-28.md`](./horizontal-scaling_2026-05-28.md) | N ∈ {1, 2, 5, 10} × LP-2 (R=100) | **SLO-H-1 4 N 위반 (부하 영역 의존성, capacity 미달)** + **SLO-H-2 4 N 통과 (max ratio +0.24%)**. PRD §I4.22 "수평 확장 SLO 의 상대성" 시스템적 한계 식별 |

### 1.2 SLI 분포 종합 (Prometheus 측, W_load 기간)

본 표는 각 LP 의 측정 결과를 단일 행으로 종합. 자세한 분포는 각 보고서 §2.

| 측정 | 영역 | SLO-1 5xx (%) | SLO-2 등록 p99 (ms) | SLO-3 전달 p99 (ms) | SLO-4 DLQ (%) | throughput (RPS) | 비고 |
|------|------|------|------|------|------|------|------|
| LP-1 baseline (R=10) | nominal capacity 미달 | 0 | 4.99 max | 9.95 max | 0 | 10.00 (k6) | Run 1/2 분산 ±0.52% |
| LP-2 normal (R=100) | nominal capacity 미달 | 0 | 4.97 max | 9.95 max | 0 | 94.21 (Prom) | 4.97 / 9.91 ms (avg) |
| LP-2 S3 (재시도+백오프) | nominal capacity 미달 | 0 | 4.99 max | 22.22 max (backoff) | 0 | 94.10 (Prom) | W3 attempts avg = 3.0 (의도 K+1) |
| LP-2 S4 (max attempts → DLQ) | nominal capacity 미달 + 의도 DLQ | 0 | 7.54 max (backlog) | n/a (success 0) | 100 (의도) | 93.48 (Prom) | C5 max_attempts_exceeded = throughput |
| LP-2 S5 (즉시 DLQ) | nominal capacity 미달 + 의도 DLQ | 0 | 4.99 max | n/a (success 0) | 100 (의도) | 94.14 (Prom) | C5 non_retriable = throughput |
| LP-3 stress (R=500, P=64KB) | **capacity 초과 (knee 진입)** | **100** | **5000 (timeout cap)** | 1620 max / 550 avg | 0 (write 거부) | **20.02 (capped)** | bound = Redis fork-time mem |
| LP-4 spike (base 100 + spike 1000 × 30s) | nominal + spike 흡수 | 0 | 9.90 max | 42.65 max (burst) | 0 | 146.5 avg | 회복 33.1초 |
| M-LOAD-5 N=1 (R=100, LP-2 normal) | nominal capacity 미달 (baseline) | 0 | ~5 | 9.92 | 0 | 94.18 (Prom) | M-LOAD-3 normal 재현성 ±0.10% |
| M-LOAD-5 N=2/5/10 (R=100) | nominal capacity 미달 (확장) | 0 | ~5 | 9.92~9.94 | 0 | 94.06~94.15 | 4 N 동일 영역 (capacity 미달) |

### 1.3 nominal capacity 미달 영역의 baseline 재현성

본 측정의 nominal 영역 (LP-1 / LP-2 normal / M-LOAD-5 N=1) 은 동일한 capacity
미달 영역이며 baseline 재현성이 ±5% 안:

- M-LOAD-3 LP-2 normal SLO-3 전달 p99 = **9.91 ms** vs M-LOAD-5 N=1 SLO-3 전달
  p99 = **9.92 ms** — Δ +0.10% (M-LOAD-5 보고서 §7.1 정합).
- M-LOAD-3 LP-2 normal throughput ≈ **94 RPS** vs M-LOAD-5 N=1 throughput =
  **94.18 RPS** — Δ <1% (M-LOAD-5 보고서 §7.1 정합).

본 재현성 확인이 §4 SLO 재조정 표의 신뢰성 근거.

---

## 2. Redis knee point 식별 (PRD `prd-phase4/04` §5.3 절차 적용)

### 2.1 knee point 측정값

본 §2.1 은 PRD §5.4 의 표 형식 정합. M-LOAD-4 LP-3 측정이 1차 증거.

| 측정 | knee point N (워커) | knee point RPS (W_load) | bound 원인 | LP-ID | 신뢰도 |
|------|---------------------|-------------------------|-------------|--------|--------|
| 1차 | N=1 (수평 확장 시도 안 함) | **~500 RPS @ P=64KB** (LP-3 부하 인가 RPS) | **단일 Redis 인스턴스의 RDB snapshot fork-time 메모리** (Docker VM 7.65GB 한계) | LP-3 | 명백 (선형성 0.040 + p99 163× + 큐 waiting 17만) |

### 2.2 bound 원인 분석 (PRD §5.3 [4] 단계 정합)

LP-3 측정의 Redis 자원 지표 (M-LOAD-4 보고서 §3.2 정합):

| 후보 bound | 임계 (PRD §5.3 [4]) | 실측 | 판정 |
|-----------|---------------------|------|------|
| Redis CPU bound | > 80% 지속 (W_load 의 90% 이상 구간) | 평균 22~73% 진동, 단일 스레드 영역의 60~73% — 한 core 의 부분 사용 | ❌ 아님 (CPU 여유 있음) |
| Redis 메모리 bound | > 80% | **메모리 % 71% 도달 후 plateau** (Docker VM 7.65GB 의 71% = ~5.43GB) | ⚠️ **plateau 영역** |
| Redis 네트워크 bound | > 호스트 네트워크 한계의 80% | (측정 안 됨) | (확인 불가) |
| 워커 자원 경합 (Redis 외) | 셋 다 아니면 | (해당 안 됨) | (해당 안 됨) |

### 2.3 본질 bound = Redis fork-time 메모리 (RDB snapshot 실패)

LP-3 측정의 결정적 증거 (M-LOAD-4 보고서 §4.4 정합):

```
ReplyError: MISCONF Redis is configured to save RDB snapshots, but it's
currently unable to persist to disk. Commands that may modify the data set
are disabled, because this instance is configured to report errors during
writes if RDB snapshotting fails (stop-writes-on-bgsave-error option).
```

**인과 경로**:

1. R=500 RPS × P=64KB → 워커 처리 capacity (~20 RPS @ concurrency=20) 와 차이
   → waiting 큐 메모리 폭증 (**1.56 GB/min**).
2. ~3분 후 Redis 메모리 ~6.4GB 도달 → Docker VM 7.65GB 한계 근접.
3. Redis 가 RDB snapshot 시도 (auto-save trigger — `save 3600 1 / 300 100 /
   60 10000` default) → **fork() 시 COW 로 일시적 ~13GB 필요** → fork 실패.
4. `stop-writes-on-bgsave-error=true` (default) 활성화 → Redis 가 **모든 write
   명령 거부** → BullMQ `queue.add` 가 거부 → API 가 500 응답.
5. 5xx 응답 100% sustained → k6 측 RPS 도달 cap (timeout 5초) + 큐는 더 이상
   자라지 못함 → sustained dead-zone.

본 bound 의 본질은 **단일 인스턴스의 RDB snapshot fork-time 메모리 압박**이며,
CPU / 네트워크 / 워커 경합이 아니다. Q-LOAD-4 (a) "Redis 단일 인스턴스 한계
식별" 정합 — 단일 인스턴스의 capacity 가 **~6.4GB 메모리 (= ~3분 W_load @
R=500 RPS × P=64KB)** 까지.

### 2.4 LP-3 knee 의 위치 한계

본 보고서는 N=1 측정의 knee point 만 1차 식별. PRD §5.5 한계:

- **호스트 사양 한계 vs Redis 한계**: 본 측정의 bound (Docker VM 메모리) 가
  Redis 자체 한계가 아닌 측정 호스트 한계의 부분 — 별도 호스트 사양에서는 knee
  position 변동 가능.
- **N 매트릭스의 거친 해상도**: N=1 단일 측정. N 함수의 knee 위치 (N=1 capacity 의 N 배
  까지 확장 가능한지) 는 capacity 미달 영역에서만 측정됨 (M-LOAD-5). LP-3
  영역의 N 매트릭스 측정은 본 PLAN 범위 외 (M-LOAD-5 보고서 §8.2 후속 권장
  cross-link).
- **시간 변동**: LP-3 의 cliff 도달 시점 (W_load 시작 후 ~3분) 은 측정 환경의
  Docker VM 상태 + Redis 메모리 fragmentation 누적에 의존.

본 한계는 §6 운영 권고에서 후속 측정 영역으로 cross-link.

---

## 3. HA / Cluster 트리거 조건 (T1~T5 명문화)

### 3.1 5 트리거 조건 (PRD `prd-phase4/04` §6.2 정합)

본 §3.1 은 PRD §6.2 의 5 트리거 조건을 본 보고서가 단일 출처로 잠근다.

| 트리거 | 측정 지표 | 임계 | 본 4단계 측정의 1차 증거 | 운영 모니터링 권고 |
|--------|-----------|------|----------------------------|----------------------|
| **T1 Redis CPU 포화** | Redis CPU 사용률 (cAdvisor 또는 `redis-cli INFO cpu`) | > 80% 지속 (1시간 윈도우의 50% 이상 구간) | **(미발화)** — LP-3 영역의 Redis CPU 22~73% 진동 (단일 스레드의 부분 사용). 본 시스템의 한계는 CPU 가 아닌 fork-time 메모리 | `redis_exporter` 도입 (별도 PR) — 본 PRD 범위 밖 (PRD §6.4 N4.6 정합) |
| **T2 Redis 메모리 포화** | Redis 메모리 사용률 | > 80% | **부분 발화** — LP-3 의 메모리 % 71% 도달 후 plateau (fork 거부로 인한 cap). 본 시스템의 절대 bound | `redis_exporter` 도입 (별도 PR) — 본 PRD 범위 밖 |
| **T3 본 시스템 knee point 가 운영 부하 안에 들어옴** | knee point RPS < 운영 평균 RPS × 2 (margin) | 운영 RPS 측정 필요 | **(미발화)** — LP-4 spike 의 회복 시간 33.1초가 base RPS 100 안에서 정상 회복. knee 가 base 영역 외 | knee point RPS 비교 — 본 보고서 §2.1 의 knee point ~500 RPS @ P=64KB 가 운영 RPS × 2 보다 작아지면 발화 |
| **T4 SLO-3 전달 지연 위반 지속** | 3단계 SLO-3 burn rate alert 14.4× 가 N 증가로도 회복 안 됨 | (`prd-phase3/04` §5.2.2 표준) | **(미발화)** — 3단계 잠정값 안. 본 측정의 4 N 모두 SLO-3 통과 (9.92ms p99 / 5000ms 잠정값의 0.2%) | 이미 3단계가 처리 — 본 보고서 추가 모니터링 0건 |
| **T5 SLO-4 DLQ 적재율 위반 지속** | 3단계 SLO-4 burn rate alert 가 N 증가로도 회복 안 됨 | (`prd-phase3/04` §5.2.3 표준) | **(미발화)** — 3단계 잠정값 안. 본 측정의 nominal 영역 모두 DLQ 0% | 이미 3단계가 처리 — 본 보고서 추가 모니터링 0건 |

### 3.2 트리거 후 진입할 후속 PRD (PRD §6.3 정합)

- **T1 / T2 발화 시:** Redis HA / Cluster PRD (자리 예약 — PLAN `09-acceptance-gates.md` §8 C-LOAD-10 정합).
- **T3 발화 시:** 운영 RPS 측정 + 본 보고서 §2.1 knee point 비교 후 동일 PRD 진입.
- **T4 / T5 발화 시:** 3단계 알람이 이미 처리. 별도 운영 PRD 의 라우팅 강화 (PRD `prd-phase4/00-overview` §3 N4.1 cross-link).

### 3.3 트리거 운영 모니터링의 본 보고서 범위

- **T1 / T2 의 `redis_exporter` 도입은 본 보고서 범위 밖** (PRD `prd-phase4/00-overview` §3 N4.6 정합 — 새 메트릭/도구 도입 0건). 별도 PR 로 후속 처리.
- **T3 의 knee point 비교는 사람 검토** (PRD §6.4 정합). 본 보고서 §2.1 표가
  운영 RPS 비교의 단일 출처.
- **T4 / T5 는 3단계 알람이 이미 처리**. 본 보고서가 추가 알람 도입 0건 (PRD
  `prd-phase4/00-overview` §3 N4.1 정합).

---

## 4. SLO 재조정 임계 산출 (Q-LOAD-9 (a) p99 × 1.5 적용)

### 4.1 baseline 영역 채택 (사용자 결정 2026-05-28 잠금)

**채택 = nominal capacity 미달 영역만 (LP-1 / LP-2 normal / M-LOAD-5 N=1)**.

근거:
- LP-3 의 capacity 초과 영역 (5xx 100% / p99 5000ms timeout cap) 은 Redis
  fork-time 메모리 cliff — SLO baseline 으로 부적절 (시스템 한계 식별의 1차
  증거이지 정상 운영 영역이 아님).
- LP-2 S3 (백오프 누적 22.22ms) / S4 (backlog 효과 7.54ms) 는 stub variant 의
  의도된 동작 — receiver 가 강제 503/400 응답을 인가했을 때의 분포라 SLO
  baseline 으로 부적절.
- LP-4 의 spike 흡수 영역 (max p99 42.65ms) 은 spike burst (1000 RPS × 30s) 의
  큐 적재 + 해제 분포 — SLO baseline 에 포함하면 평상 운영 신호가 약화.
- nominal 영역 (capacity 미달 + steady-state) 만이 SLO 의 의미 보존 영역이며,
  본 보고서 §1.3 의 재현성 ±5% 안 확인이 신뢰성 근거.

### 4.2 SLO 재조정 임계 표 (PRD `prd-phase4/03` §5.1 형식 정합)

| SLO | 3단계 잠정값 (`prd-phase3/04` §3.1) | 실측 baseline (nominal 영역, 가장 보수적 max) | 재조정 임계 (p99 × 1.5 또는 §4.5 변형) | 비고 |
|-----|---------------|-------------------------|------|------|
| **SLO-1 가용성** | 5xx 비율 ≤ **0.5%** | **0%** (LP-1 / LP-2 normal / M-LOAD-5 N=1 / LP-4 base 모두 0) | **0.5% 유지** (PRD §4.5 변형: 실측 0 → 3단계 잠정값 유지) | LP-3 의 100% 는 capacity 초과 영역 — baseline 제외 |
| **SLO-2 등록 지연** | p99 ≤ **0.5s (500ms)** | **4.99ms** (LP-1 Run 1 max 4.990ms / LP-1 Run 2 max 4.964ms / LP-2 normal max 4.972ms / M-LOAD-5 N=1 ~5ms) — 가장 보수적 max | **7.5ms** (4.99 × 1.5 = 7.485 ms 반올림) | 3단계 잠정값 대비 **~67×** 공격적 재조정 — nominal 영역 SLI 분포 정합 |
| **SLO-3 전달 지연** | p99 ≤ **5s (5000ms)** | **9.96ms** (LP-1 Run 1 max 9.941ms / LP-1 Run 2 max 9.959ms / LP-2 normal max 9.946ms / M-LOAD-5 N=1 9.92ms) — 가장 보수적 max | **14.9ms** (9.96 × 1.5 = 14.94 ms 반올림) | 3단계 잠정값 대비 **~335×** 공격적 재조정 — nominal 영역 SLI 분포 정합 |
| **SLO-4 DLQ 적재율** | ≤ **1%** | **0%** (LP-1 / LP-2 normal / LP-4 / M-LOAD-5 4 N 모두 0) | **1% 유지** (PRD §4.5 변형: 실측 0 → 3단계 잠정값 유지) | LP-2 S4/S5 의 100% 는 stub variant 의 의도된 동작 — baseline 제외 |

### 4.3 SLO 재조정의 함의

**SLO-2 / SLO-3 의 공격적 재조정 (각 ~67× / ~335×)** 이 시사하는 바:

1. **3단계 SLO 잠정값이 의도적으로 매우 보수적** (운영 부하 측정 전 채택된
   "안전한 큰 값") — 본 측정으로 nominal 영역의 실제 SLI 분포가 잠정값의 ~2%
   영역에 머무름이 확인됨.
2. **본 재조정 임계 (7.5 ms / 14.9 ms) 가 의미 있는 회귀 감지 능력 제공** — 본
   시스템의 nominal 영역 SLI 가 회귀로 50% 이상 악화될 때 알람 발화. Google
   SRE Workbook 의 p99 × 1.5 권고와 정합.
3. **SLO-1 / SLO-4 의 잠정값 유지** 는 nominal 영역의 실측이 0 이라 §4.5 변형
   적용 정합. 운영 영역에서 5xx / DLQ 가 0 이상으로 발생하기 시작하면 잠정값이
   회귀 감지의 첫 신호로 작동.

### 4.4 갱신 금지 항목 (PRD §5.3 + 3단계 I6.1 / I6.2 정합)

본 재조정 표가 **숫자만** 갱신 대상이며, 다음 항목은 변경되지 않는다:

- SLI PromQL 형태 (메트릭 이름 / 라벨 / 집계 함수).
- 측정 윈도우 (`[5m]` / `[1h]` / 28d / 7d / 1d).
- burn rate 표준값 (14.4× / 6×, I6.2).
- 알람 규칙 YAML 의 구조 (`docker/prometheus/rules/*.yaml` 의 group / for /
  labels / annotations).
- 메트릭 이름 / 라벨 enum (3단계 I3.1 / I3.2).

### 4.5 갱신 인계 (단계 3, 본 PLAN commit 시퀀스 외)

본 §4.2 의 갱신 임계가 §A SLO 임계 갱신 PR template (단계 2 commit 에서 인라
인) 의 입력. 실제 갱신 PR 의 발행은 본 PLAN commit 시퀀스 외 별도 commit 시리
즈 — 사용자 단계 3 진행 (§6.3 인계 절차 정합).

---

## 5. SLO-H-1 / SLO-H-2 검증 결과 종합 (PRD `prd-phase4/04` §4)

### 5.1 SLO-H-1 (α=0.8 처리량 선형성) — 4 N 모두 위반

목표: `throughput(N) ≥ throughput(1) × N × 0.8` (Q-LOAD-10 (b))

baseline: throughput(1) = **94.1836 RPS** (M-LOAD-5 보고서 §3 정합)

| N | throughput(N) | target (≥) | linearity = throughput(N) / (throughput(1) × N) | SLO-H-1 |
|---|---------------|-------------|--------------------------------------------------|---------|
| 2 | 94.1005 | 150.6938 | **0.4996** | ❌ 위반 |
| 5 | 94.1536 | 376.7345 | **0.1999** | ❌ 위반 |
| 10 | 94.0634 | 753.4691 | **0.0999** | ❌ 위반 |

**위반 사유 = 부하 영역 의존성 (capacity 미달, PRD §I4.22 정합)**:

- R=100 LP-2 nominal 부하가 **N=1 워커 capacity (~500 jobs/s @ concurrency=5
  × 처리 시간 ~10ms)** 의 ~20% → 추가 워커가 idle 상태.
- linearity 가 **정확히 1/N 패턴** (N=2 → 0.5, N=5 → 0.2, N=10 → 0.1) — 부하
  영역 의존성의 결정적 signal.
- 자원 경합 / Redis 포화 / cgroup 과소 모두 아님 (M-LOAD-5 보고서 §3.1 분석
  정합).

### 5.2 SLO-H-2 (β=1.2 p99 안정성) — 4 N 모두 통과

목표: `p99(N) ≤ p99(1) × 1.2` (Q-LOAD-11 (b))

baseline: p99(1) = **9.9156ms** (worker processing duration p99, M-LOAD-5
보고서 §4 정합)

| N | p99(N) | target (≤) | ratio = p99(N) / p99(1) | SLO-H-2 |
|---|--------|-------------|-------------------------|---------|
| 2 | 9.9198ms | 11.8987ms | **1.0004** | ✅ 통과 |
| 5 | 9.9397ms | 11.8987ms | **1.0024** | ✅ 통과 |
| 10 | 9.9195ms | 11.8987ms | **1.0004** | ✅ 통과 |

**4 N 모두 통과** — 최대 ratio +0.24% (M-LOAD-5 보고서 §4 정합). N=10 의
cgroup over-commit 우려 영역 (14 컨테이너 / 12 core) 도 p99 안정.

### 5.3 SLO-H-1 / SLO-H-2 의 비대칭 결과 해석 (시스템적 한계 식별)

본 검증 결과의 비대칭성 — **SLO-H-1 4 N 위반 + SLO-H-2 4 N 통과** — 의 본질은
**부하 영역에 따른 SLO 의미 보존성 차이** (PRD §I4.22 "수평 확장 SLO 의 상대성"
시스템적 한계):

| SLO 종류 | 부하 영역 의존성 | capacity 미달 영역의 평가 |
|----------|------------------|----------------------------|
| **SLO-H-1 (처리량 선형성)** | 강함 | throughput 이 R 함수로 cap → linearity = 1/N → 자동 위반 (검증 의미 없음) |
| **SLO-H-2 (p99 안정성)** | 약함 | p99 의 절대값 비교 → capacity 미달/초과 모두에서 의미 있음 (본 측정에서 안정성 명확 확인) |

**본 측정의 의미 있는 산출물:**

- ✅ **SLO-H-2 4 N 통과** — 본 시스템이 수평 확장 시 p99 안정성을 명확히 보장
  (β=1.2 임계 안에서 ratio +0.24% — 매우 보수적). 본 결과가 본 시스템의 **수평
  확장 능력에 대한 정량적 약속의 1차 증거**.
- ⚠️ **SLO-H-1 의 의미 보존 검증은 capacity 초과 영역의 측정 필요** — 본 PLAN
  범위 외. 후속 PRD/마일스톤 영역 (§6.2 cross-link).

### 5.4 정확도 한계 (PRD `prd-phase4/04` §R4.18 정정 + M-LOAD-5 §5 정합)

M-LOAD-5 fix `db23169` 이후 `docker/prometheus.yml` 의 worker scrape 가 single
static target → `docker compose --scale worker=N` (N>=2) 시 docker compose
service-level DNS round-robin 으로 매 scrape (15s) 마다 N 인스턴스 중 1 응답
→ `rate()` PromQL 의 N 합산 정확도 저하 가능성.

**본 측정의 round-robin scrape 영향 분리 불가:**

- 4 N throughput 동일 (94.06~94.18 RPS) — capacity 미달이 본질이라 round-robin
  영향과 분리 불가능.
- N=1 측정은 round-robin 없음 (단일 인스턴스) → baseline 신뢰 가능.
- SLO-H-2 의 p99 안정성도 동일 한계 — 부하 영역 영향 + round-robin 영향이
  분리 불가. 다만 ratio +0.24% 는 두 영향 합산이 매우 작음을 시사.

**정확도 한계 개선은 본 PLAN 범위 외** — `prometheus.yml dns_sd_configs` 도입
정량화는 후속 작업 (§6.2 cross-link).

---

## 6. 운영 권고 사항 + 후속 작업 cross-link

### 6.1 HA / Cluster 도입 우선순위

본 보고서 §3.1 의 T1~T5 트리거 조건의 우선순위 (1차 증거 + 운영 발화 위험):

| 우선순위 | 트리거 | 1차 증거 | 운영 발화 위험 |
|---------|--------|-----------|----------------|
| **최우선** | **T2 Redis 메모리 포화** | LP-3 영역의 fork-time 메모리 cliff (Docker VM 7.65GB 의 71% plateau) | **높음** — 운영 부하의 R / P 가 LP-3 영역에 진입하면 T2 발화 → write 거부 / 5xx 100% |
| 중간 | T3 본 시스템 knee point | LP-3 의 knee point ~500 RPS @ P=64KB | 중간 — 운영 RPS 가 ~250 RPS 이상 (knee 의 50%) 도달하면 후속 PRD 진입 권장 |
| 낮음 | T1 Redis CPU 포화 | (미발화) — 단일 스레드의 부분 사용 | 낮음 — 본 시스템의 한계가 CPU 가 아님 |
| 낮음 | T4 / T5 SLO-3 / SLO-4 burn rate | (미발화) — 3단계 알람이 처리 | 낮음 — 본 보고서 추가 모니터링 0건 |

**권고:** 운영 부하 측정 + T2 (Redis 메모리) 모니터링 도입 (`redis_exporter`)
을 가장 먼저 후속 PR 로 처리. T2 발화 시 Redis HA / Cluster PRD 진입.

### 6.2 SLO 임계 갱신 PR 의 인계 절차 (단계 3, 본 PLAN commit 시퀀스 외)

본 §6.2 는 PRD `prd-phase4/03` §6 정합. 단계 3 은 본 PLAN commit 시퀀스 외
별도 commit 시리즈로 사용자가 직접 진행한다.

**갱신 대상 (§A PR template 의 §2 단일 출처):**

1. **`docs/prd-phase3/04-slo-and-alerts.md` §3.1 임계 숫자만 갱신:**
   - SLO-1 가용성: 5xx 비율 ≤ **0.5%** (잠정값 유지 — §4.2 정합).
   - SLO-2 등록 지연: p99 ≤ **7.5ms** (4.99 × 1.5).
   - SLO-3 전달 지연: p99 ≤ **14.9ms** (9.96 × 1.5).
   - SLO-4 DLQ 적재율: ≤ **1%** (잠정값 유지 — §4.2 정합).
2. **`docs/prd-phase3/04-slo-and-alerts.md` §3.4 "잠정값" 표기 → "실측 기반
   갱신 (2026-05-28, `docs/prd-phase4/results/final_2026-05-28.md`)"** (C-LOAD-7
   정합).
3. **`docker/prometheus/rules/*.yaml` 의 PromQL 임계 숫자 동시 갱신** (C-LOAD-8
   정합):
   - `webhook-relay-availability.yaml` — burn rate `> (14.4 * 0.005)` 의 `0.005`
     유지 (SLO-1 잠정값 유지).
   - `webhook-relay-latency.yaml` — SLO-2 / SLO-3 의 임계 숫자 (0.5s / 5s → 0.0075s
     / 0.0149s) 갱신.
   - `webhook-relay-dlq.yaml` — `0.01` (1%) 유지.
   - `webhook-relay-platform.yaml` — 변경 0건.

**갱신 금지 항목 (PRD §5.3 + 3단계 I6.1 / I6.2 / I3.1 / I3.2 정합):**

- SLI PromQL 형태 / 측정 윈도우 / burn rate 표준값 / 알람 YAML 구조 / 메트릭
  이름 / 라벨 enum.

### 6.3 4단계 후속 PRD 권고 (본 PRD 범위 밖)

본 4단계 PLAN 묶음 종료 후 다음 PRD 의 진입이 자연스럽다:

| 후속 PRD | 진입 트리거 | 본 보고서의 인계 항목 |
|----------|------------|------------------------|
| **Redis HA / Cluster PRD** | T2 (Redis 메모리 포화) 운영 발화 | 본 §2 의 knee point 정밀 위치 + bound 원인. PLAN `09-acceptance-gates.md` §8 C-LOAD-10 자리 예약 |
| **Capacity 초과 영역 부하 측정 PRD** | SLO-H-1 의 실효 검증 필요 | 본 §5.3 의 부하 영역 의존성 식별. capacity 초과 영역 (R=500 + P=small 1KB 또는 R=1000+) × N 매트릭스 — 부하 프로필 재설계 필요 |
| **Prometheus scrape 정확도 PRD** | 본 §5.4 의 round-robin scrape 정확도 한계 정량화 | `prometheus.yml dns_sd_configs` 도입 + 동일 측정으로 single target vs DNS SD 의 정확도 비교 |
| **카오스 엔지니어링 PRD** | 운영 발화 — 부하 + 워커 강제 종료 결합 | 본 PRD `prd-phase4/00-overview` §3 N4.3 cross-link |
| **운영 배포 자동화 PRD** | 운영 인프라 도입 시 | 본 PRD §3 N4.5 cross-link |

### 6.4 M-LOAD-5 cross-link 후속 작업 4건 (별도 commit 시리즈 인계)

본 §6.4 는 직전 세션 HANDOFF.md 가 식별한 "M-LOAD-6 진입 전 권장 절차" 4건 중
본 보고서가 직접 처리하지 않는 항목 (사용자 결정 2026-05-28 잠금 — 후속 작업
으로 분리). 본 PLAN closeout 후 사용자가 별도 commit 시리즈로 진행.

| 항목 | 내용 | 후속 처리 권장 |
|------|------|----------------|
| #1 T1/T2/T3 트리거 종합 | 본 보고서 §3.1 에 포함 ✅ | (본 보고서 안에서 처리됨) |
| #2 Capacity 초과 영역 N 매트릭스 | LP-3 의 Redis fork cliff 영역 → 부하 프로필 재설계 필요 (R=500 + P=small 1KB 고정 등) | 별도 PRD (본 §6.3 의 "Capacity 초과 영역 부하 측정 PRD" cross-link) |
| #3 `prometheus.yml` dns_sd 도입 정량화 | M-LOAD-5 의 single target round-robin scrape 영향 (본 §5.4) 의 정량화 | 별도 PRD (본 §6.3 의 "Prometheus scrape 정확도 PRD" cross-link) |
| #4 `architecture.md` line 35 historical decision 정리 | `architecture.md` 의 "worker /metrics 호스트 포트 3001 사용으로 3002 로 이동, 2026-05-27 결정" 코멘트가 M-LOAD-5 fix `db23169` 이후 historical 표현 — 정리 권장 | PLAN closeout 후 별도 commit (C-LOAD-11 cross-link, `09-acceptance-gates.md` §8) |

**본 PLAN commit 시퀀스 안에서는 위 4건 모두 처리하지 않는다** (PLAN §5
"절대 만들지/수정하지 않는 것" 정신 = 코드/PRD/3단계 PLAN 변경 0건). 본 보
고서는 후속 작업의 cross-link 단일 출처만 제공.

---

## 7. PLAN closeout 체크리스트

본 §7 은 PLAN `07-m-load-6-redis-knee-and-final-report.md` §10 의 closeout
체크리스트 정합. **단계 4 commit 에서 [ ] → ✅ 로 mark-as-done**.

### 7.1 마일스톤별 Exit Criteria 통과

- [ ] M-LOAD-1 Exit Criteria 전건 통과 (`02-m-load-1-bootstrap.md` §6).
- [ ] M-LOAD-2 Exit Criteria 전건 통과 (`03-m-load-2-lp1-baseline.md` §6).
- [ ] M-LOAD-3 Exit Criteria 전건 통과 (`04-m-load-3-lp2-nominal.md` §6).
- [ ] M-LOAD-4 Exit Criteria 전건 통과 (`05-m-load-4-lp3-lp4.md` §6).
- [ ] M-LOAD-5 Exit Criteria 전건 통과 (`06-m-load-5-horizontal-scaling.md` §6).
- [ ] M-LOAD-6 Exit Criteria 전건 통과 (`07-m-load-6-redis-knee-and-final-report.md` §6).

### 7.2 결과 보고서 6건 commit

- [ ] `docs/prd-phase4/results/` 에 LP-1 / LP-2 / LP-3 / LP-4 / horizontal-scaling
  / final 결과 보고서 6 commit.

### 7.3 회귀 가드 그린 유지

- [ ] 1~3단계 IT + UT 전건 그린 유지 (37 files / 194 passed @ 6bf67c4 시점 기준).
- [ ] `pnpm typecheck` 0 errors.
- [ ] `IT-R1-domain-boundary` 그린 (core boundary grep 0 hits).
- [ ] `docker compose config` 유효.

### 7.4 PRD AC + 본 PLAN 범위 보호

- [ ] PRD `prd-phase4/00-overview` §5 AC4.0~AC4.7 의 모든 항목 만족.
- [ ] `packages/streams-internals/` 변경 0건.
- [ ] outline 3 파일 (`README.md`, `00-decisions-needed.md`, `01-milestones.md`)
  변경 0건.
- [ ] `docs/prd-phase4/00~05.md` 본문 변경 0건.
- [ ] `docs/architecture.md`, `CLAUDE.md`, `README.md` 본문 변경 0건 (PLAN
  closeout 후 별도 PR — C-LOAD-1 / C-LOAD-4 / C-LOAD-9 / C-LOAD-14 / C-LOAD-15).

### 7.5 SLO 임계 갱신 PR 인계

- [ ] 본 보고서 §6.2 절차에 따라 단계 3 의 SLO 임계 갱신 PR 인계 — 별도 commit
  시리즈로 진행. `prd-phase3/04` §3.1 + `docker/prometheus/rules/*.yaml` 의 임계
  숫자만 갱신. SLI PromQL 형태 / 측정 윈도우 / burn rate / 알람 YAML 구조 변경
  0건.

### 7.6 결정 보존

- [ ] 1~2단계 Resolved 21건 보존 (`docs/plan/00-decisions-needed.md`).
- [ ] 3단계 Resolved 15건 보존 (`docs/plan-phase3/00-decisions-needed.md`).
- [ ] 4단계 PRD Resolved 13건 보존 (`docs/prd-phase4/00-decisions-needed.md`).
- [ ] 4단계 PLAN Open 0건 (`docs/plan-phase4/00-decisions-needed.md`).

---

## §A SLO 임계 갱신 PR template

> **단계 2 commit 의 단일 출처.** 본 절은 단계 2 에서 PLAN
> `07-m-load-6-redis-knee-and-final-report.md` §11 본문을 그대로 인라인.
> 본 PLAN commit 시퀀스 안에서는 placeholder 상태. 단계 2 commit 이 본 절을
> 채운다.

(단계 2 commit 에서 인라인 예정)
